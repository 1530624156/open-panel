<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mavis.mypanel.dao.TServerUserTemplateDao">

    <resultMap type="com.mavis.mypanel.entity.TServerUserTemplate" id="TServerUserTemplateMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="type" column="type" jdbcType="INTEGER"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="pemAttachmentUuid" column="pem_attachment_uuid" jdbcType="VARCHAR"/>
        <result property="remark" column="remark" jdbcType="VARCHAR"/>
    </resultMap>

    <!-- 批量插入 -->
    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into main.t_server_user_template(nametypeusernamepasswordpem_attachment_uuidremark)
        values
        <foreach collection="entities" item="entity" separator=",">
        (#{entity.name}#{entity.type}#{entity.username}#{entity.password}#{entity.pemAttachmentUuid}#{entity.remark})
        </foreach>
    </insert>
    <!-- 批量插入或按主键更新 -->
    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into main.t_server_user_template(nametypeusernamepasswordpem_attachment_uuidremark)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}#{entity.type}#{entity.username}#{entity.password}#{entity.pemAttachmentUuid}#{entity.remark})
        </foreach>
        on duplicate key update
name = values(name) type = values(type) username = values(username) password = values(password) pem_attachment_uuid = values(pem_attachment_uuid) remark = values(remark)     </insert>

</mapper>

